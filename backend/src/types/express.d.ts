declare global {
  namespace Express {
    // Extend the Express Request object here when needed (e.g. req.user after auth)
    // interface Request {
    //   user?: { id: string };
    // }
  }
}

export {};
